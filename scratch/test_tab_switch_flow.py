# Let's inspect what happens with Streamlit session state lifecycle
# In Streamlit, when key is not in session_state and value is omitted:
# slider defaults to min_value
# number_input defaults to min_value

print("Verifying Streamlit widget lifecycle behavior...")
